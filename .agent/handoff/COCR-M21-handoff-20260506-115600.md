# Handoff: COCR-M21 — Commercial OCR Blocked (Credential Missing)

## Context

- branch: `mimo`
- commit: `5be7193`
- date: `2026-05-06 11:56 CST`
- previous handoff: `.agent/handoff/real-commercial-ocr-provider-integration-20260506-041527.md`

## Route Taken

**Route C** — credential still missing. Both Baidu and Tencent OCR keys are absent from all checked sources (shell env, root/backend/pdf-service `.env`, alternative env files, backend DB).

## What Was Done This Session

1. Full site survey: git state, reports, handoffs, debug dirs, credential sources
2. Confirmed 2 uncommitted changes from previous session were reasonable fixes
3. Committed checkpoint: `5be7193 fix(pdf-service): clear skipped_reason when provider status is ok/partial`
4. Ran all baseline validations (17 pytest, tsc, build, script syntax) — all PASS
5. Generated env-diagnosis.masked.txt with full credential audit
6. Generated M21 BLOCKED phase report
7. Generated this handoff

## Current State

- **git status**: clean
- **commercial OCR**: BLOCKED (key_missing for both providers)
- **tests**: 17 passed, all builds green
- **bbox_source**: still `tesseract_local_ocr` (no commercial data to promote with)
- **workbench E2E**: 1 test listed, not runnable (no commercial fixture)

## Scripts Ready (Verified Working)

| Script | Purpose | Status |
|---|---|---|
| `run_real_commercial_ocr_smoke.py` | Single-page smoke | Ready |
| `run_real_commercial_ocr_batch.py` | Batch 1/6/16 | Ready |
| `compare_commercial_vs_local_bbox.py` | Bbox comparison | Ready |
| `provider_health_report.py` | Health report | Ready |
| `start-commercial-ocr-stack.sh` | E2E stack | Fixed, ready |
| `data-analysis-commercial-ocr-workbench.spec.ts` | Playwright E2E | Ready |

## What Needs to Happen Next

### Immediate (P0) — Blocked on User
1. User provides `BAIDU_API_KEY` + `BAIDU_SECRET_KEY` or `TENCENT_SECRET_ID` + `TENCENT_SECRET_KEY`
2. Run single-page smoke → batch → bbox compare → fixture → workbench E2E
3. See `docs/commercial-ocr-phase-reports/COCR-M21-commercial-ocr-blocked-credential-missing.md` for exact commands

### Can Continue Without Key (P2/P3)
- Provider error classification tests (auth_failed, quota_exhausted, timeout, empty_result)
- env diagnosis script enhancement
- BLOCKED report auto-generation
- whole-page understanding / semantic grouping / recrop-plan artifact format definitions
- similarity schema and API design
- review frontend UI improvements on local OCR data
- question_versions / audit_events / rights metadata schema design

## Debug Products

```
debug/commercial-ocr/20260506-115600/
├── git-status.txt
├── env-diagnosis.masked.txt
├── test-results.txt
```

## Reports Written

- `docs/commercial-ocr-phase-reports/COCR-M21-commercial-ocr-blocked-credential-missing.md`
- `.agent/reports/COCR-M21-blocked-report.md`
- This handoff

## Do NOT Repeat

- Do NOT re-check credentials — all sources have been exhaustively audited
- Do NOT retry smoke scripts without new credentials — they will return `key_missing` again
- Do NOT pretend tesseract_local_ocr is commercial OCR
- Do NOT generate mock pass results

## Resume Prompt

```
你是本项目 Agent，从 COCR-M21 续跑。
分支: mimo, commit: 5be7193
状态: 商业 OCR BLOCKED (key_missing), 基础验证全部通过
上次报告: docs/commercial-ocr-phase-reports/COCR-M21-commercial-ocr-blocked-credential-missing.md
Handoff: .agent/handoff/COCR-M21-handoff-20260506-115600.md

如果用户已提供 credential:
1. 先跑单页 smoke (见 handoff 里的续跑命令)
2. 再跑 batch 1/6/16
3. 再跑 bbox compare
4. 生成 commercial fixture
5. 跑 workbench E2E
6. 在真实 commercial bbox 下重跑 VLM/LLM 预审核

如果 credential 仍缺失:
- 继续不依赖 key 的 P2/P3 增强任务
- 或等用户提供 key

不要从头重做。直接从现场状态继续。
```
